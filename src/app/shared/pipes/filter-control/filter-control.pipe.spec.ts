import { FilterControlPipe } from './filter-control.pipe';

describe('FilterControlPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterControlPipe();
    expect(pipe).toBeTruthy();
  });
});
